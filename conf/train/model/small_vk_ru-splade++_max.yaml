# @package _global_

init_dict:
  # базовая модель
  model_type_or_dir: deepvk/RuModernBERT-small
  # базовая модель для query
  model_type_or_dir_q: null
  freeze_d_model: 0
  # тип аггрегации (сумма, max и т.д.)
  agg: max
  fp16: false

config:
  tokenizer_type: deepvk/RuModernBERT-small
  # тип модели (splade, splade-doc и т.д.)
  matching_type: splade

