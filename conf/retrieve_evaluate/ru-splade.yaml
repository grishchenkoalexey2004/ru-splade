# @package _global_


data:
  Q_COLLECTION_PATH: [data/dev_queries]
  EVAL_QREL_PATH: [data/dev_qrel.json]

config:
  top_k: 1000
  threshold: 0
  eval_metric: [[mrr_10, recall]]
  retrieval_name: [shrinked_dataset]
